class Solution {
    public boolean rotateString(String s, String goal) {
        // Rotations are only valid if lengths are equal
        if (s.length() != goal.length()) {
            return false;
        }
        // s + s contains all possible rotations of s
        String doubled = s + s;
        return doubled.contains(goal);
    }
}task2
